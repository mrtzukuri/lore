<%=  simple_form_for @user_bookmark = UserBookmark.new do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id %>
  <%= f.hidden_field :learning_opportunity_id, value: lo.id %>
  <%= f.button :submit, class: 'fa fa-star' %>
<% end %>
